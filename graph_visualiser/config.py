import os

# Main directory containing subdirectories with graphs.
# The application will scan this directory for subdirectories (collections).
GRAPH_DATA_PATH = "data"

# Filename pattern for graph files to search for within collections
GRAPH_FILE_PATTERN = "*_graph.pkl"
